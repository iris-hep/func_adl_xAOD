[pytest]
junit_family=legacy
addopts = -m "not atlas_xaod_runner and not atlas_r22_xaod_runner and not cms_aod_runner and not cms_miniaod_runner and not atlas_releases_runner"
markers = 
  atlas_xaod_runner: Run ATLAS R21 xAOD tests that use docker to do the C++ compile and short runs
  atlas_r22_xaod_runner: Run ATLAS R22 xAOD tests that use docker to do the C++ compile and short runs
  atlas_releases_runner: Run parameterized ATLAS releases tests that validate templates and CMAKE across multiple AnalysisBase versions
  cms_aod_runner: Run CMS AOD tests that use docker to do the C++ compile and short runs
  cms_miniaod_runner: Run CMS miniAOD tests that use docker to do the C++ compile and short runs
